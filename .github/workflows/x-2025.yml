name: X 2025推文抓取
on:
  workflow_dispatch: {}

jobs:
  fetch-2025:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write
      actions: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: 安装依赖
        run: pip install -r requirements.txt

      - name: 抓取2025推文（单轮18分钟软停，exit 42=需续跑）
        env:
          TWITTER_AUTH_TOKEN: ${{ secrets.TWITTER_AUTH_TOKEN }}
        run: |
          set +e
          python x_fetch_2025.py
          code=$?
          echo "EXIT_CODE=$code" >> $GITHUB_ENV
          exit 0

      - name: 提交状态与数据（带重试）
        if: always()
        run: |
          git config user.name "monitor-bot"
          git config user.email "action@github.com"
          git add data/aoying_2025_state.json data/aoying_2025_tweets.json
          git diff --cached --quiet || git commit -m "抓取X 2025推文(续跑)"
          for i in 1 2 3 4; do
            git pull --rebase -q || true
            if git push; then echo "push成功"; break; fi
            echo "push第${i}次失败,重试"
            sleep 5
          done

      - name: 未完成则自动续跑
        if: env.EXIT_CODE == '42'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          curl -s -X POST \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/${{ github.repository }}/actions/workflows/x-2025.yml/dispatches \
            -d '{"ref":"main"}'
          echo "已触发下一轮"